import { _M } from "../geometry/_m"

// DEBUG FLAGS ************************************* / 

export const IS_DEV_START_ORBIT = false

const PHISICS_CONF = {
    IS_DEBUG: false
}

// ************************************************/ 

export const CONSTANTS = {
    PHISICS_CONF
}
